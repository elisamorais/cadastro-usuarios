1 - Nos testes
    "o usuario naoExiste não existe no sistema"
    'o usuario raupp não deve existir mais no sistema'
    'deveria excluir o usuario raupp'

    Não deveria ser necessário tentar validar o userSchema para requisições onde o usuário não existe ou exclusão de usuários
    Correção: O trecho abaixo foi removido

    "expect(res.body).to.be.jsonSchema(userSchema);"

2- libs adicionadas
    koa-static
    Para conseguir incluir o CSS e o javascript no front

    koa-bodyparser
    Para conseguir processar os dados que chegam no servidor

    modal.css
    apenas um css separado que foi usado pra criar um modal

3 - A função isTestMode foi retirada do stackoverflow par auxiliar nos testes

4 - Segui dois tutoriais para implementar o sqlite, um para entender como funciona e o outro para aprender a usar com async await.